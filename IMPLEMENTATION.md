# Wood Block Puzzle - 구현 문서

## 🎯 구현 개요

Wood Block Puzzle 게임의 핵심 로직을 C#으로 구현하였습니다.

## 📂 스크립트 구조

```
@Scripts/
├── Core/                    # 핵심 시스템
│   ├── GameManager.cs      # 게임 전체 흐름 관리
│   ├── BoardManager.cs     # 8x8 보드 관리, 블록 배치, 라인 체크
│   └── BlockManager.cs     # 블록 생성 및 관리
│
├── Data/                    # 데이터 정의
│   ├── BlockData.cs        # 블록 모양 데이터베이스 (17가지 블록)
│   └── GameData.cs         # 게임 진행 데이터, 설정
│
├── Gameplay/                # 게임플레이 로직
│   ├── Cell.cs             # 보드 셀 (개별 타일)
│   └── Block.cs            # 드래그 가능한 블록
│
├── Managers/                # 기능별 매니저
│   ├── ScoreManager.cs     # 점수, 콤보 시스템
│   ├── SoundManager.cs     # BGM, 효과음 관리
│   └── UIManager.cs        # UI 표시 및 애니메이션
│
└── Utils/
    └── Constants.cs        # 게임 상수 정의
```

## 🎮 핵심 기능

### 1. 게임 보드 시스템 (BoardManager)
- **8x8 그리드** 생성 및 관리
- 블록 배치 가능 여부 검증
- 월드 좌표 ↔ 그리드 좌표 변환
- 가로/세로 라인 완성 체크
- 라인 제거 애니메이션

### 2. 블록 시스템 (Block, BlockManager)
- **17가지 블록 모양** 정의 (1x1, 2x2, 3x3, L자, T자, Z자 등)
- 드래그 앤 드롭 인터페이스
- 3개 블록 슬롯 관리
- 블록 배치 후 자동 리필

### 3. 점수 시스템 (ScoreManager)
- 블록 배치 시 점수 (셀 개수 × 10점)
- 라인 제거 보너스 (줄당 100점)
- 콤보 시스템 (연속 라인 제거 시 1.5배 배율)
- 최고 기록 자동 저장

### 4. 사운드 시스템 (SoundManager)
- BGM 재생
- 블록 배치 효과음 (2가지 변형)
- 라인 제거 효과음 (4가지 변형)
- 콤보 효과음 (5단계)
- 최고 기록 갱신 사운드

### 5. UI 시스템 (UIManager)
- 현재 점수 / 최고 기록 표시
- 콤보 애니메이션
- 게임 오버 패널
- 설정 패널 (볼륨 조절)
- DOTween 애니메이션 적용

### 6. 게임 흐름 (GameManager)
- 게임 시작/재시작
- 게임 오버 체크
- 매니저 간 통합 및 이벤트 처리

## 🔧 사용 기술

- **UniTask**: 비동기 라인 제거 처리
- **DOTween**: UI 및 블록 애니메이션
- **TextMesh Pro**: 텍스트 렌더링
- **Unity Input System**: 터치/마우스 입력
- **Singleton Pattern**: 매니저 클래스

## 🎯 게임 로직

### 블록 배치
1. 사용자가 블록을 드래그
2. 보드 위치로 이동
3. 배치 가능 여부 검증
   - 그리드 범위 내인지 확인
   - 모든 셀이 비어있는지 확인
4. 배치 성공 시:
   - 블록 셀 활성화
   - 점수 추가
   - 사운드 재생
   - 라인 체크

### 라인 제거
1. 가로줄 8칸 완성 체크
2. 세로줄 8칸 완성 체크
3. 완성된 라인 발견 시:
   - 매칭 효과 표시 (200ms)
   - 제거 애니메이션 (300ms)
   - 점수 추가 + 콤보 증가
   - 사운드 재생

### 게임 오버
1. 블록 배치 후 항상 체크
2. 현재 활성화된 3개 블록 중
3. **어느 블록도 보드에 배치할 수 없으면** 게임 오버
4. 최고 기록 저장

## 📝 구현 특징

### ✅ 장점
- **깔끔한 아키텍처**: Core, Data, Gameplay, Managers로 분리
- **확장성**: 새로운 블록 모양 추가 용이
- **재사용성**: 각 매니저가 독립적으로 동작
- **이벤트 기반**: ScoreManager → UIManager 이벤트 통신
- **비동기 처리**: UniTask로 라인 제거 처리
- **애니메이션**: DOTween으로 부드러운 효과

### 🔄 개선 가능 사항
- 오브젝트 풀링 (블록 재사용)
- 파티클 효과 추가
- 난이도 시스템 (블록 크기 증가 등)
- 저장/불러오기 기능 확장
- 멀티플랫폼 입력 최적화

## 🚀 다음 단계

1. **Unity 에디터에서 설정**:
   - MainScene에 GameManager 추가
   - 스프라이트 할당 (Cell, Block, Shadow)
   - UI 요소 배치
   - 사운드 클립 할당

2. **테스트**:
   - 블록 배치 테스트
   - 라인 제거 테스트
   - 게임 오버 조건 테스트
   - 점수/콤보 시스템 테스트

3. **최적화**:
   - 프로파일링
   - 메모리 최적화
   - 모바일 성능 테스트

## 📌 참고 사항

- Unity Editor Version: **6000.2.72f2**
- Target Platform: **Android**
- 모든 스크립트는 `WoodBlock` 네임스페이스 사용
- Singleton 패턴으로 매니저 접근성 확보
